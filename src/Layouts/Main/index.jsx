import React from 'react';
import Header from '../Header';
import Footer from '../Footer';

const MainContainer = () => {
    return (
        <div>
            <Header />
            <p>This is main MainContainer</p>
            <Footer />
        </div>
    );
};

export default MainContainer;